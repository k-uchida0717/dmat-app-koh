# DMAT災害医療支援プラットフォーム — 導入ガイド

このフォルダ一式で、**オンラインでもオフラインでも使えるアプリ（PWA）**として運用できます。
将来的な**ネイティブアプリ化**の手順もこの文書の後半に記載します。

## フォルダの中身

| ファイル | 役割 |
|---|---|
| `index.html` | アプリ本体（データ・資料・全機能を内蔵） |
| `manifest.json` | アプリ情報（名前・アイコン・表示方法） |
| `sw.js` | Service Worker（オフライン動作の心臓部） |
| `icon-192.png` / `icon-512.png` | アプリアイコン |
| `apple-touch-icon.png` | iOS用アイコン |

**重要：この5〜6ファイルは必ず同じフォルダに一緒に置いてください。**

---

## 使い方は2通り

### A. オンライン公開して使う（推奨・更新が楽）

Webに置いてURLで配ると、全員が最新版にアクセスでき、各自の端末にインストールできます。

#### 手順（GitHub Pagesの例・無料）
1. GitHubアカウントを作成し、新しいリポジトリ（例：`dmat-app`）を作る
2. このフォルダの中身を全部アップロード（`index.html`, `manifest.json`, `sw.js`, アイコン類）
3. リポジトリの Settings → Pages → Source を「main / root」に設定して保存
4. 数分後に `https://ユーザー名.github.io/dmat-app/` が発行される
5. そのURLをスマホ／PCで開く

> GitHub Pages以外でも、Netlify・Cloudflare Pages・Vercel・自組織のWebサーバーなど、**HTTPSで配信できる場所ならどこでも**動きます。Service WorkerはHTTPSまたはlocalhostでのみ動作します。

#### インストール（各利用者の操作）
- **Android / PC（Chrome・Edge）**：右下に出る「⬇ アプリをインストール」ボタン、またはアドレスバーのインストールアイコンをタップ
- **iPhone / iPad（Safari）**：共有ボタン → 「ホーム画面に追加」

インストール後はホーム画面のアイコンから、**ブラウザのバーなしの全画面アプリ**として起動します。

### B. ローカルファイルとして使う（サーバー不要）

USB・共有フォルダ等でこのフォルダごと配り、各自の端末に保存します。

- **簡易利用**：`index.html` をダブルクリックしてブラウザで開くだけでも、データ閲覧・入力・PDF出力・資料閲覧は動きます。
  - ただし `file://` で開くと Service Worker（自動オフラインキャッシュ）は動きません。地図タイル等の初回取得もできません。
- **ローカルでもPWAをフル機能で使いたい場合**：簡易HTTPサーバーで開きます。
  ```
  # Pythonが入っていれば、フォルダ内で：
  python -m http.server 8000
  # ブラウザで http://localhost:8000/ を開く
  ```
  これで Service Worker が有効になり、一度開けば以降オフラインでも完全動作します。

---

## オフラインでどこまで動くか

| 機能 | オフライン | 備考 |
|---|---|---|
| 全体俯瞰・医療機関・DMAT隊・検索・詳細 | ✅ | データ内蔵 |
| クロノロ・ToDo・連絡先・現状分析の入力/PDF | ✅ | |
| 様式・資料（PDF/Word/Excel）の閲覧・保存 | ✅ | アプリ内に埋込済 |
| 地図（一度表示したエリア） | ✅ | タイルをキャッシュ |
| 地図（未訪問エリア）／外部リンク／Googleマップ | ❌ | オンライン時のみ |
| 自動保存（指定ファイルへ上書き） | ✅ | PC版Chrome/Edge |

**運用のコツ**：出動前にオンラインで一度アプリを開き、使いそうな地域の地図を表示してキャッシュさせ、最新のEMIS/DMATデータを取り込んでおく。現地で電波が切れても、その状態から活動を続けられます。

---

## 更新のしかた

- **公開運用（A）**：サーバー上の `index.html` 等を新しいものに差し替えるだけ。利用者が次に開いたとき、Service Workerが更新を検知し「新しいバージョンがあります」と表示します。
- キャッシュを強制的に切り替えたいときは、`sw.js` の先頭 `CACHE_VERSION` を `dmat-v1` → `dmat-v2` のように上げると、古いキャッシュが破棄されます。

---

## ネイティブアプリ化（Windows/Mac/iOS/Android のインストール型アプリ）

このPWAは、ほぼそのまま各OSのネイティブアプリに包めます。目的別に2つの道があります。

### 道1：スマホアプリ（iOS / Android）→ Capacitor
Webの資産をそのままアプリ化し、ストア配布や端末機能利用ができます。

```
npm install -g @capacitor/cli
npm init -y
npm install @capacitor/core @capacitor/ios @capacitor/android
npx cap init "DMAT支援" "jp.dmat.support" --web-dir=.
# このフォルダ（index.html等がある場所）を web-dir に指定
npx cap add ios
npx cap add android
npx cap open android   # Android Studioでビルド
npx cap open ios       # Xcodeでビルド（Macが必要）
```
- iOSアプリ配布にはApple Developer Program（年間登録）、AndroidはGoogle Play Consoleが必要です。
- 組織内配布のみなら、Android は APK直接配布、iOS は Ad Hoc/組織内配布も可能です。

### 道2：デスクトップアプリ（Windows / Mac / Linux）→ Tauri
軽量なインストーラ型デスクトップアプリになります。ファイル保存（自動保存）との相性も良好です。

```
# Rust と Node が必要
npm create tauri-app@latest
# フロントエンドに「既存のHTML/CSS/JS」を選び、このフォルダを指定
npm run tauri build   # .msi / .dmg / .AppImage が生成される
```

### どちらを選ぶか
- 現場スタッフがスマホ・タブレットで使う中心なら **Capacitor（道1）**。
- 本部PCでの入力・保存が中心なら **Tauri（道2）**。
- まずは **PWA（この状態）** で運用を始め、必要になってからネイティブ化するのが最も手戻りが少ない進め方です。PWAで作り込んだものがそのまま資産になります。

---

## 注意（災害運用として）
- クロノロ・連絡先・現状分析の入力は端末に保存されますが、**正本はエクスポート／自動保存したファイル**です。こまめに保存し、交代時はファイルで引き継いでください。
- 医療機関・DMAT隊の**変動データは必ずEMIS／DMATモニターの公式ファイルを正**とし、本アプリはそれを取り込んで俯瞰・記録するビューアとして使ってください。表示値は取込時点のスナップショットです。
